# Healthcare Analytics Lab - Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to git (it's in .gitignore)

# MySQL Database Configuration
MYSQL_ROOT_PASSWORD=your_secure_password_here
MYSQL_DATABASE=healthcare_analytics_lab
MYSQL_USER=root

# Jupyter Configuration (optional)
JUPYTER_TOKEN=your_jupyter_token_here

# Production Notes:
# - Use strong passwords (min 16 chars, mixed case, numbers, symbols)
# - Store production secrets in AWS Secrets Manager / Azure Key Vault
# - Rotate credentials every 90 days
# - Never use 'root' in production - create service accounts with minimal privileges
